from setuptools import setup

setup(
      name='gym_chess',
      version='0.0.1',
      install_requires=['gym', 'anytree', 'tabulate', 'termcolor', 'PyQt5', 'pytest'],  # dependencies required
      py_modules=['gym_chess']
)
